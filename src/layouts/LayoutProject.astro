---
interface Props {
	title: string;
}

const { title } = Astro.props;
import { projects } from "@cv";
import type { getEntry } from "astro:content";
// const positions = projects.map(project => project[Object.keys(project).position[0]]);
// const positions = projects[0] ? projects[0].name : undefined;
// const urlParams = new URLSearchParams(window.location.search);
// const greetingValue = urlParams.get("greeting");
// console.log(greetingValue);
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<!-- <title>TBO</title> -->
	</head>
	<body>
		{
			// projects.map((i) => {
			// 	return (
			// 		<a href="">{i[0].position}</a>
			// 	)
			// })

			projects.map(
				({
					position,
					url,
					description,
					highlights,
					name,
					isActive,
					preview,
					preview_url,
					github,
				}) => {
					return (
						// const myarray = projects.filter((el, i) => (i == 0)).map((element, index)=> {
						<a href="">{position === 0 ? name : ""}</a>
					);
				},
			)
		}
		<slot />
		<main>
			<div></div>
			<footer>
				<!-- {positions} -->
			</footer>
		</main>
	</body>
</html>
<style is:global>
	html {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			"Courier New",
			"Courier New",
			Courier,
			monospace;
		background: #eee;
		letter-spacing: -0.025rem; /* espacio entrelas letras reducido */
	}
	body,
	figure {
		margin: 0;
		padding: 0;
	}
	a {
		text-decoration: none;
	}
	ul {
		list-style: none;
		margin: 0;
		padding: 0;
	}
	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}
	h1,
	h2,
	h3,
	h4 {
		margin: 0;
		font-family:
			system-ui,
			-apple-system,
			BlinkMacSystemFont,
			"Segoe UI",
			Roboto,
			Oxygen,
			Ubuntu,
			Cantarell,
			"Open Sans",
			"Helvetica Neue",
			sans-serif;
	}
	p {
		color: #676;
		font-size: 0.8rem;
		line-height: 1.5;
		margin: 0;
		text-wrap: pretty;
	}
</style>
