---
import Section from "@/components/Section.astro";
import { projects } from "@cv";
---

<!-- <h2>Project</h2> -->
<Section title="Proyectos">
    <!-- <a href="/indexProjects">about Sonali</a> -->
    <ul>
        {
            projects.map(
                ({
                    position,
                    url,
                    description,
                    highlights,
                    name,
                    isActive,
                    preview,
                    preview_url,
                    github,
                }) => {
                    return (
                        <li>
                            <article>
                                <header>
                                    <h3>
                                        <a
                                            href={url}
                                            target="_blank"
                                            title={`Ver el proyecto ${name}`}
                                        >
                                            {name}
                                        </a>
                                        {isActive && <span> &bull;</span>}
                                        {github && (
                                            <a
                                                class="github-code-link"
                                                href={github}
                                                target="_blank"
                                                title={`Ver código fuente del proyecto ${name}`}
                                            />
                                        )}
                                    </h3>
                                    <p>{description}</p>
                                </header>
                                <footer>
                                    <ul>
                                        <imput id="a_pos">{position}</imput>
                                        {preview && (
                                            <button type="button" id="a_btn">
                                                Ver proceso
                                            </button>
                                        )}
                                        {highlights.map((highlight) => {
                                            return <span>{highlight}</span>;
                                        })}
                                    </ul>
                                </footer>
                            </article>
                        </li>
                    );
                },
            )
        }
    </ul>
</Section>

<script>
    import { redirectToDefaultLocale } from "astro:i18n";

    function onSend(val: string) {
        alert(val);
    }
    // onClick="onSend(postion)"

    // const onSend = (id) => {
    //     console.log(`id: ${id}`);
    //     console.log(id);
    //     let tmpTodos = [...todosArr];

    //     tmpTodos.splice(id, 1);

    //     setTodosArr(tmpTodos);
    // };

    // const onSend = (event: any, pos: any) => {
    //     alert(event);
    //     alert(pos);
    // };
    // let arg1 = event.target.getAttribute('data-arg1');

    // function gotoNode(val: any) {
    //     alert(val);
    // }

    // href={preview_url}

    // name="gotoNode"
    // data-arg1={position}

    // onclick="selPos('${position}')"

    // const a_pos = document.getElementById("a_pos");
    // console.log(a_pos);

    // localStorage.setItem("key_pos", {position})
    // const form = document.getElementById('form');
    // form.addEventListener('submit', function(e) {
    //     e.preventDefault();
    // const posVal = a_pos.value;
    // });
    // const form = document.getElementById;
    //     window.onload = function() {
    //    var getInput = prompt(${position});
    //    localStorage.setItem("storageName",getInput);
    // }
    //     var position
    //     function save_position(pos: any){
    //      position = pos;
    //     position = document.getElementById('a_btn');
    // }
</script>
<style>
    ul {
        /* Responsive sin media queries */
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        /* margin-inline: -16px; */
    }
    #a_btn {
        font-weight: bold;
        font-size: medium;
        position: relative;
        text-align: center;
        border-radius: 6px;
        background: #acc;
        border: 1px solid;
        /* block px-2 py-2 transition hover:-color: ;dark:hover:text-yellow-400 */
    }
    #a_btn:hover {
        text-decoration: none;
        background: #acf;
        color: #fff;
    }
    article {
        border-radius: 8px;
        border: 2px solid #cdc;
        gap: 16px;
        display: flex;
        flex-direction: column;
        padding: 16px;
        height: 100%;
    }
    article header {
        flex: 1;
    }
    article a {
        color: #444;
    }
    article a:hover {
        text-decoration: underline;
    }
    article p {
        font-size: 0.75rem;
        line-height: 1.2rem;
        margin-bottom: 4px;
    }
    article h3 {
        margin-bottom: 4px;
    }
    article h3 span {
        color: rgb(0, 188, 64);
        animation-name: flicker;
        animation-duration: 3s;
        animation-timing-function: ease-in;
        animation-iteration-count: infinite;
    }
    footer {
        /* display: flex; */
        display: inline-block;
        flex-wrap: wrap;
        gap: 4px;
        font-size: 0.6rem;
        /* padding-left: 10px; */
    }
    footer span {
        /* display: inline-flex; */
        text-align: center;
        border-radius: 6px;
        background: #acc;
        color: #444;
        font-size: 0.6rem;
        font-weight: 500;
        padding: 0.2rem 0.6rem;
        margin-right: 6%;
    }
</style>
